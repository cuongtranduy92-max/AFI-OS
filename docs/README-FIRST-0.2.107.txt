AFI-OS 0.2.107 — CLAUDE ĐỌC TERMS

CÀI ĐẶT MỘT LẦN
1. Nhấp đúp UPDATE-AFI-OS-0.2.107.command.
2. Chờ dòng “Cập nhật an toàn đã hoàn tất”.
3. Trong thư mục AFI-OS, nhấp đúp SETUP-LLM.command.
4. Dán Anthropic API key khi Terminal hỏi rồi nhấn Enter.

CÁCH DÙNG
1. Vào Tìm dự án và nhập domain.
2. Bấm Kiểm tra lại tự động hoặc Trích Terms bằng Claude.
3. Đọc câu trích dẫn và mở link nguồn.
4. Bấm ✓ Chấp nhận khi đúng hoặc ✗ Bỏ khi sai.
5. Chỉ dữ kiện đã chấp nhận mới được dùng tính hoàn vốn/chấm điểm.

AN TOÀN
API key chỉ nằm trong macOS Keychain, không nằm trong database, .env hoặc repo.
Claude không tự mở quyền PPC, không loại dự án, không dừng campaign và không ghi
Google Ads. Hoa hồng “up to” chỉ để tham khảo, không dùng tính hoàn vốn.

Nếu cần quay lại, chạy ROLLBACK-AFI-OS-0.2.107.command trong thư mục AFI-OS.
